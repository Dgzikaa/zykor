# Handoff — sessão 23/06/2026 (financeiro: DFC, DRE, Orçamentação, Planejamento)

> Contexto pro chat novo continuar rápido. Último commit: **`fe3eeefd`** (depois deste handoff terá mais 1).
> **Regra de ouro:** o usuário (Rodrigo) autoriza aplicar em prod/migrations/cron/frontend sem pedir; verificar tsc/lint/build no fim. Executar, não perguntar.

## ⚠️ #1 — DEPLOY É O MAIOR BLOQUEADOR
Quase todo "bug" reportado é **lag de deploy**: muita mudança de frontend está **commitada mas não promovida em produção**. Mudança de **banco/RPC/edge** vale na hora; **frontend só depois do "Promote to Production" na Vercel** (build ≠ produção). Sempre lembrar o usuário de promover o último commit.

## 🔴 FIOS ABERTOS (fazer no chat novo, em ordem)

### 1. Troca de bar em TODAS as telas (S.O.S do usuário)
- Causa: telas que recebem `initialData` (SSR) inicializam o estado 1x e **não re-sincronizam** quando troca de bar (troca é client-side via `BarContext`, que já faz `router.refresh()`).
- **Fix-padrão** (já aplicado em `OrcamentacaoClient.tsx`, commit `fe3eeefd`): `useEffect` com `useRef(barId)` que chama a função de carregar quando `selectedBar.id` muda (guard evita re-fetch no mount).
- **Telas com `initialData` a auditar/corrigir igual:** `planejamento-comercial/components/PlanejamentoClient.tsx`, `desempenho/components/DesempenhoClient.tsx`, `organizador/components/OrganizadorClient.tsx`, `visao-mensal/page.tsx`. (DRE e DFC já buscam por `useEffect` com dep `selectedBar` — OK.)

### 2. Item 4.2 — editar Custo Artístico/Prod de evento FUTURO vira REALIZADO (bug ativo)
- No modal do `planejamento-comercial`, editar "Custo Artístico" salva em `c_art` (REAL, via `/api/eventos/[id]/valores-reais`) → aparece como realizado (ex: 10k), **sem o ⚠️**.
- **O usuário quer:** pra evento futuro (ex: 24.06 que ainda vai acontecer), editar o artístico/produção deve alterar a **PROVISÃO** = `c_artistico_plan` (mostra ⚠️ projeção), **NÃO** o realizado. O CA substitui pelo real quando o evento acontecer.
- Já existe a base (item 4.2 no `planejamento-service.ts`): `c_art = real>0 ? real : (c_artistico_plan>0 ? c_artistico_plan : c_art_projecao)`. **Falta:** o modal, pra evento futuro, gravar SÓ `c_artistico_plan` e **não** mandar `c_art` pro valores-reais (ou mandar 0). Idem produção (mas NÃO existe `c_producao_plan` no eventos_base — só `c_artistico_plan`; pode precisar criar a coluna análoga).
- **404 junto:** `PUT /api/eventos/1086` deu "Evento não encontrado" embora o evento EXISTA (eventos_base id=1086, bar 3, 24.06). O `valores-reais` funcionou (gravou c_art=10k) mas o `/api/eventos/[id]` 404. Provável: header `x-selected-bar-id` com bar errado (contexto de bar) — ligado ao #1. Conferir `getBarIdFromRequest` vs o evento.

### 3. Item 3 — Escritório Central % do faturamento
- Feito 4% **fixo no código** (`orcamentacao-service.ts`: `if (s.nome === 'Escritório Central') { plan = 0.04*fatPlan; proj = 0.04*fatProj }`). Usuário pode querer **editável na tela** (% na planilha).

### 4. Consumações nos outros blocos
- Despesas Comerciais já = DRE (commit `593f1789`). Faltam alinhar (se o usuário pedir): `[Consumação] Sócios` (Não Operacionais), `[Consumação] Funcionários Operação/Escritório` (Despesas Administrativas) — hoje no de-para com nomes "Consumação Sócios/Func", sem linha na ESTRUTURA desses blocos.

## ✅ JÁ FEITO NESTA SESSÃO (não refazer)

### DFC furo (o grande achado) — RESOLVIDO
- Parcela paga ficava **sem baixa** no bronze → DFC (por baixa conciliada) não contava → ≠ CA.
- Causa: edge `contaazul-baixas` extraía a baixa de menos formatos (`array/.itens`) do que lia a data (`extrairDataPagamento`: 5 formatos). Objeto único/`{baixas}` → pegava a data mas não gravava a baixa, e marcava synced (nunca re-tentava). 43 parcelas.
- Fix: alinhar a extração (edge **v4 deployada**) + backfill (requeue `baixas_synced_em=NULL`) + cron `watchdog-baixas-furo` (08:50) que auto-cura. **Validado:** Deboche SALÁRIO FUNCIONÁRIOS mai/26 23.765,45 → **25.421,69 = CA**. Memória: `feedback_dfc_furo_baixa_formato_resposta`.

### De-para POR BAR + matching robusto
- `bar_id` nullable nos 3 de-paras (`meta.categoria_dfc_map`, `financial.dre_categoria_macro`, `meta.categoria_zykor_map`): NULL=global, bar_id=exceção. Unique `(categoria, coalesce(bar_id,0))`.
- `public.normcat(text)` = MAIÚSCULA+sem acento+trim (IMMUTABLE, unaccent). DFC/DRE casam por normcat.
- **Perf:** normcat dentro de LATERAL re-normaliza o de-para por linha → timeout (DFC e DRE ficaram em branco/500). Fix: resolver o de-para 1x num **CTE** (`DISTINCT ON` + override) + hash join. Memória `feedback_unaccent_lateral_timeout`. Ambos RPCs já corrigidos.
- **Aba "Categorias" no DFC** = gerenciador self-service: todas as categorias do CA + dropdown classifica `grupo_dfc` por bar (`meta.set_categoria_dfc`). RPC `financial.get_dfc_categorias`.

### Orçamentação (itens dos sócios)
1 Produção Mensal Fixo da DRE · 2 Consumação=DRE (+R$152k AJUSTE CMV no CMV) · 3 Escritório Central 4% · 4 proj Atrações/Produção=Σ planejamento · 4.1 projeção exclui dias zerados (`projetar_custos_pre_lancado`) · 4.2 base feita (ver fio #2) · 5 somatório alinhado por coluna · 6 % do faturamento na proj (sub-linhas e grupo macro) · empilhamento <hoje · **Faturamento Meta**: Plan=manual, Proj=Σ M1, Real=Σ real_r · `normKey` (case/acento no goldCat) · `nomePorBar` (Deboche="Administrativo Local") · Despesas Comerciais = DRE.

### Planejamento-comercial
- 2 bares com colunas IDÊNTICAS (Custo Artístico, **Custo Produção, $ Couvert**, %Art/Fat, Couv/Art, Consumação) — antes era condicional por bar.
- `calculate_evento_metrics` agora computa `c_prod` do bar 4 (era `0` cravado); fica 0 até ter lançamento em "Produção de Eventos".
- Item 5: tfoot alinhado.

## CONTEXTO-CHAVE
- DFC = `public.get_dfc_por_ano(bar,ano,só_conciliado)` por baixa; default **só conciliado** (extrato do Gonza, id_reconciliacao). Página `/financeiro/dfc`.
- DRE = `public.get_dre_por_ano(bar,ano)` competência, bronze ao vivo. Página `/financeiro/dre`.
- Orçamentação lê `gold.orcamento_realizado_mensal` (via `meta.categoria_zykor_map` → silver → gold; refresh `public.cron_refresh_gold_orcamentacao_diario()`). ESTRUTURA curada em `orcamentacao-service.ts`.
- Bares: 3 Ordinário, 4 Deboche. SEMPRE filtrar bar_id.
- **Chaves Stone:** o usuário disse "deixa, tá tranquilo" (não rotacionar). `scripts/credenciais-stone.local.json` gitignored.
- project_id Supabase: `uqtgsvujwcbymjmvkjhy`.
