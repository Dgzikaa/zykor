fix(desempenho): service filtra clamp 9999s como null

Quando silver.tempos_producao tem outliers ou falta de dados, 
ETL aplica LEAST(x, 9999). Se valor exato = 9999, significa 
clamp ativado por dado sujo upstream ou AVG(vazio)=NULL. 

Service agora retorna null (UI mostra "-") em vez de exibir 
166 min absurdo.

Fix defensivo. Causa raiz S14/26: todos t0_t3=0 no silver.
